<!-- HERO SECTION -->
<section class="relative min-h-[700px] flex items-center justify-center py-20 overflow-hidden bg-gradient-to-br from-surface via-surface-dim to-primary/5">
  <div class="container mx-auto px-6 relative z-10">
    <div class="text-center mb-10">
      <span class="inline-block px-3 py-1 bg-primary/10 text-primary text-[10px] font-black uppercase tracking-widest rounded-full mb-4">E-Commerce Público</span>
      <h1 class="text-5xl md:text-7xl font-black text-on-surface leading-tight tracking-tight mb-4">
        MAYOR DE REPUESTOS <span class="text-primary">LA CIMA</span>
      </h1>
      <p class="text-stone-600 text-lg max-w-2xl mx-auto">Localice piezas críticas con precisión absoluta por VIN, OEM o aplicación.</p>
    </div>
    <!-- Search Console -->
    <div class="bg-white p-1 rounded-xl shadow-2xl max-w-4xl mx-auto">
      <div class="grid grid-cols-1 md:grid-cols-4 gap-px bg-outline rounded-lg overflow-hidden">
        <div class="bg-white p-4">
          <label class="block text-[10px] font-bold text-stone-500 uppercase mb-2">Año</label>
          <select class="w-full border-none bg-transparent font-bold text-sm focus:ring-0">
            <option>Seleccionar Año</option>
            <option>2024</option>
            <option>2023</option>
          </select>
        </div>
        <div class="bg-white p-4">
          <label class="block text-[10px] font-bold text-stone-500 uppercase mb-2">Marca</label>
          <select class="w-full border-none bg-transparent font-bold text-sm focus:ring-0">
            <option>Seleccionar Marca</option>
            <option>Toyota</option>
            <option>Ford</option>
            <option>Caterpillar</option>
          </select>
        </div>
        <div class="bg-white p-4">
          <label class="block text-[10px] font-bold text-stone-500 uppercase mb-2">Modelo</label>
          <select class="w-full border-none bg-transparent font-bold text-sm focus:ring-0">
            <option>Seleccionar Modelo</option>
            <option>Hilux</option>
            <option>Land Cruiser</option>
          </select>
        </div>
        <div class="bg-primary p-2 flex items-center justify-center">
          <button class="w-full bg-stone-900 text-white font-black text-xs uppercase tracking-widest py-4 rounded-lg hover:bg-stone-800 transition-colors">
            <span class="material-symbols-outlined text-lg mr-2">search</span>
            Buscar Repuesto
          </button>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- B2B BANNER -->
<section class="py-16 bg-stone-900 text-white">
  <div class="max-w-7xl mx-auto px-6 flex flex-col md:flex-row items-center justify-between gap-8">
    <div>
      <h2 class="text-3xl font-black uppercase tracking-tight mb-2">¿Eres Taller o Flota?</h2>
      <p class="text-stone-400 text-sm">Acceso a precios de mayorista y stock en tiempo real.</p>
    </div>
    <a href="/registro_corporativo_b2b.html" class="bg-primary text-stone-900 font-black px-8 py-4 rounded-lg uppercase tracking-widest hover:bg-primary-light transition-colors">
      Entrar al Portal ERP
    </a>
  </div>
</section>

<%- include('../partials/scripts') %>
